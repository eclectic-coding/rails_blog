require "rails_helper"

RSpec.describe UsersHelper, type: :helper do
  # it "queries for User gravatar image" do
  #   user = create(:user)
  #   gravatar_gateway = double()
  #   allow(gravatar_gateway).to receive(gravatar_for(user)).and_return(/secure.gravatar.com/)
  #
  #   expect(response).to eq(200)
  # end
end
